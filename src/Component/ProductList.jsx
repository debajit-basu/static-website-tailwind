import React from "react";
import { useNavigate } from "react-router-dom";

export default function ProductList() {
  const navigate = useNavigate();

  return <div className="container">Product list page</div>;
}
